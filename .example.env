# generate with openssl rand -hex 32
SESSION_SECRET=...
ENCRYPTION_KEY=...

# Database configuration (for PostgreSQL container)
DB_USER=safehaven
DB_PASSWORD=change_this_password
DB_NAME=safehaven

# Full database URL (optional - will be constructed from above if not set)
# DATABASE_URL=postgres://safehaven:password@db:5432/safehaven
